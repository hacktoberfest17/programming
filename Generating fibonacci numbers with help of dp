#include<bits/stdc++.h>
using namespace std;
int memo[10000];
int fib(int n)
{

    if (n<=0) return 0;
    else if (n==1) return 1;
     else if(memo[n]) return memo[n];
    memo[n] = fib(n-1)+fib(n-2);
    return memo[n];
}

int main()
{
for(int f=0;f<30;f++)
    cout<<fib(f);
}
