// The forEach loop executes a provided function once for each array element.

var states = ["Kano", "Adamawa", "Jigawa"];

states.forEach(function(state) {
  console.log(state + " is a state in Nigeria.");
});
