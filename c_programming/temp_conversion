#include <stdio.h>

int main(){
  int celsius,kelvin;
  int lower, upper, step;

  celcius = lower = 0;
  upper = 100;
  step = 5;
  
  printf("Celsius\tKelvin\n");
  while(celsius <= upper){
    kelvin = celsius+273;
    printf("3%d\t3%d\n", celsius, kelvin);
    celsius = celsius+step;
  }
}

